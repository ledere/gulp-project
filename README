Gulp Project
============

Getting started:

Make sure you have node and npm installed
Run npm install in the project folder

Setting up your local development server:

run 'gulp setup' to set up dependencies and the initial build
run 'gulp' to run the server

go to 'localhost:8080' to see your project in action